/* Riverdi STM32 Evaluation Board: the PlatformIO builder force-includes
 * variant_<BOARD>.h; the generic variant carries everything (gated on
 * ARDUINO_GENERIC_F469IITX from the board manifest). */
#include "variant_generic.h"
